/*
const bhe = jest.mock('../dist/bhe.js');
console.log(bhe);
describe('BetterHTMLElement constructor argument errors', () => {
    test("more than one wrapper args throws MutuallyExclusiveArgs", () => {
        elem({byid: "foo", query: '.bar'});

    });


});*/
